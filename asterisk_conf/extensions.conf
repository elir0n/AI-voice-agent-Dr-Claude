[general]
static=yes
writeprotect=no
autofallthrough=yes

[voicebot]
exten => 1000,1,NoOp(🤖 Incoming call to AI Agent)
 same => n,Answer()
 same => n,Wait(1)
 same => n(start),AGI(agi://voice-agent:3000/voice)
 same => n,GotoIf($["${AGISTATUS}"="HANGUP"]?end:start)
 same => n(end),Hangup()
